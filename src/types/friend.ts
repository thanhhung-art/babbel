export interface Friends {
  id: string;
  userId: string;
  friendId: string;
  createdAt: Date;
  udpateAt: Date;
}

export interface FriendRequest {
  id: string;
  userId: string;
  friendId: string;
  createdAt: Date;
  udpateAt: Date;
}